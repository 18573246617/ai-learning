# 学习进度快照（2026-08-17）

> 用途：换新对话时，让新助手快速了解"学到哪了、还剩什么、怎么继续"。新对话里直接说："先读 D:\AI学习\学习进度快照.md，然后继续帮我学习第 4 周。"

---

## 一、你是谁、目标是什么

- 在职前端开发者（会 JS/React），目标是转岗 **AI 应用开发 / Agent 开发**
- 全程 TypeScript，主学 Express 5，后续 PostgreSQL + Prisma + LLM 应用 + Agent
- 总计划在 `D:\AI学习\学习计划\AI应用开发转岗学习计划.md`（36 周，5 个阶段）
- 目前处于**阶段一（Node.js 后端）第 4 周**，总进度约 8%

## 二、项目结构（一个统一的学习项目）

项目根目录：`D:\AI学习\代码练习\阶段一-Node.js后端\`

```
├── package.json          # 统一依赖（pnpm 管理，type: module）
├── tsconfig.json         # strict + esModuleInterop + types:["node"]
├── pnpm-workspace.yaml   # allowBuilds: esbuild
├── env/.env.development  # 环境变量（--env-file 加载）
├── 参考答案/week2/       # week2 参考实现（先自己写，卡住再看）
└── src/
    ├── week1/   # TS 基础、事件循环、原生 http（已完成）
    ├── week2/   # Express 主应用：待办 CRUD + auth 模块（进行中/有遗留）
    ├── week3/   # fs、流、pipe、SSE、环境变量、crypto（已完成）
    └── week4/   # 本周：bcrypt、JWT、Bearer、pino（空目录，进行中）
```

常用命令（在项目根目录）：

- `pnpm dev:week2`（端口 1300，主应用）
- `pnpm dev:week3`（week3 练习）
- `pnpm check`（类型检查）

## 三、已完成的内容

### 第 1 周 ✅（计划已打勾）

- 环境：Node LTS、pnpm、tsx、TypeScript
- TS 基础：interface / type / 泛型 / 联合类型
- 事件循环：宏任务 / 微任务执行顺序
- 原生 http 服务器：路径路由、请求体事件、响应头

### 第 2 周 ✅ 技能项已打勾，产出有 2 个遗留 bug（见"待办"）

- Express 5：路由、路径参数、查询参数、中间件（`express.json()`、自定义日志）
- **自己手写了一个 JSON 解析中间件 myJson**（理解 express.json 原理）
- zod 校验：`safeParse`、`z.coerce`、`z.enum`、`transform`、`refine`、对象剥字段行为
- 分层结构：routes / data / middleware / schemas；app.ts 与 index.ts 分离
- 待办 CRUD 接口（内存数组）+ auth 模块（注册/登录/用户信息）
- 认证：sha256 哈希密码、randomBytes 生成 token、token 存哈希入库、checkAuth 中间件（挂 req.user）

### 第 3 周 ✅（计划已打勾）

- fs：readFile vs createReadStream、writeFile、pipe 复制文件
- 背压概念
- SSE：`/events` 接口（text/event-stream + setInterval 分段推送）已跑通
- 环境变量：process.env、.env、`node --env-file`
- crypto：sha256、randomBytes（并在 auth 里实际用上了）
- yaml 配置读取（装了 yaml 包）

## 四、待办 / 遗留问题（按优先级）

1. **week2 两个 schema bug（必须收尾）**
   - `src/week2/schemas/tasks/index.ts`：
     - `updateTaskSchema` 缺 `completed: z.boolean().optional()`
     - `listTasksSchema` 的 completed 用了 `z.boolean()`，但 query 参数是字符串，要改成 `z.enum(["true","false"]).transform(v => v === "true").optional()`
   - `src/week2/data/tasks/index.ts`：`updateTask` 参数类型缺 `completed`
2. **app.ts 缺 404 兜底**（notFoundHandler 应挂在所有路由之后、错误处理之前）
3. **第 4 周进行中**（本周主题）
   - 密码：sha256 → **bcrypt**（加盐 + 慢哈希）
   - token：随机 token → **JWT**（`jwt.sign` / `jwt.verify`，expiresIn 过期）
   - 请求头：`x-http-token` → **Authorization: Bearer** 规范
   - 日志：console.log → **pino** 结构化日志
   - 需安装：`pnpm add bcrypt jsonwebtoken pino` + `pnpm add -D @types/jsonwebtoken`
   - `JWT_SECRET` 必须放 `.env`，用 `--env-file` 加载
   - **新增（生产级标准）**：搭建 ESLint + Prettier（lint/format/check 脚本）；新建 config 模块用 zod 校验环境变量；错误处理升级为自定义错误类 + 统一 errorHandler + 404 兜底
4. 第 5 周预告：vitest + supertest 测试、multer 文件上传、部署上线

## 五、学习方法约定（重要）

- **生产级标准**：所有学习产出必须能直接写进生产，代码规范与架构对齐市场上最主流的 Node.js + TypeScript 工程实践。基准文档：`学习计划/生产级代码规范与架构标准.md`，从第 4 周起严格执行（ESLint + Prettier 提前到本周）
- 每周：只给**学习笔记 + 练习要求**，不给成品代码；卡住 15 分钟以上再要提示或参考实现
- 参考答案在 `参考答案/week2`，写完后或卡住时对照
- 养成习惯：写完自查（防复制粘贴 bug）、每周收尾闭环、git 提交
- 学习者特点：动手能力强、会串联知识（crypto 用到 auth）、有安全意识（token 存哈希），但偶尔有复制粘贴 bug 和"旧账不清"的习惯，需要提醒闭环

## 六、下次对话的交接语

> 我是正在转岗 AI 应用开发的前端开发者，已经完成第 1-3 周学习（第 4 周进行中）。请先读 `D:\AI学习\学习进度快照.md`，再读 `D:\AI学习\学习笔记\阶段一-Node.js后端\第4周学习笔记.md`，我们继续学第 4 周：bcrypt、JWT、Bearer、pino。先帮我把第 4 周笔记里的练习 1（bcrypt）和练习 2（JWT）写完并验收。
