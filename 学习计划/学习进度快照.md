# 学习进度快照（2026-08-28）

> 用途：换新对话时，让新助手快速了解"学到哪了、还剩什么、怎么继续"。新对话里直接说："先读 D:\AI学习\学习进度快照.md，然后继续帮我学习第 6 周。"

---

## 一、你是谁、目标是什么

- 在职前端开发者（会 JS/React），目标是转岗 **AI 应用开发 / Agent 开发**
- 全程 TypeScript，主学 Express 5，后续 PostgreSQL + Prisma + LLM 应用 + Agent
- 总计划在 `D:\AI学习\学习计划\AI应用开发转岗学习计划.md`（36 周，5 个阶段）
- 目前处于**阶段一（Node.js 后端）第 6 周（最后一周）**，总进度约 15%

## 二、项目结构（一个统一的学习项目）

项目根目录：`D:\AI学习\代码练习\阶段一-Node.js后端\`

```
├── package.json          # 统一依赖（pnpm 管理，type: module）
├── tsconfig.json         # strict + esModuleInterop + types:["node"]
├── pnpm-workspace.yaml   # allowBuilds: esbuild + bcrypt
├── env/.env.development  # 环境变量（--env-file 加载，已移出 git 跟踪）
├── eslint.config.js      # ESLint flat config（typescript-eslint）
├── .prettierrc.json      # Prettier 配置（无分号、单引号、4 空格缩进）
├── .github/workflows/ci.yml  # 第 6 周练习 7：GitHub Actions（待建）
├── 参考答案/week2/       # week2 参考实现（先自己写，卡住再看）
└── src/
    ├── week1/   # TS 基础、事件循环、原生 http（已完成）
    ├── week2/   # ★ Express 主应用：本周做四层重构（routes/controllers/services/repositories）
    ├── week3/   # fs、流、pipe、SSE、环境变量、crypto（已完成）
    └── week4/   # bcrypt/JWT/pino 练习（已完成，主逻辑已并入 week2）
```

常用命令（在项目根目录）：

- `pnpm dev:week2`（端口 1300，主应用）
- `pnpm test`（vitest，18 个用例）
- `pnpm lint`（ESLint）/ `pnpm format`（Prettier）/ `pnpm check`（tsc --noEmit）
- `pnpm build:week2` + `node dist/week2/index.js`（生产构建/启动）

> ⚠️ TypeScript 7 与 typescript-eslint 不兼容的官方解法：`typescript` 别名指向 `@typescript/typescript6`（供 ESLint），`@typescript/native` 保留原生 tsc 7。改依赖时别动这两条别名。

## 三、已完成的内容

### 第 1 周 ✅

- 环境：Node LTS、pnpm、tsx、TypeScript
- TS 基础：interface / type / 泛型 / 联合类型
- 事件循环：宏任务 / 微任务执行顺序
- 原生 http 服务器：路径路由、请求体事件、响应头

### 第 2 周 ✅

- Express 5：路由、路径参数、查询参数、中间件
- 手写 JSON 解析中间件 myJson（理解 express.json 原理）
- zod 校验：safeParse、z.coerce、z.enum、transform、refine
- 待办 CRUD + auth 模块、checkAuth 中间件

### 第 3 周 ✅

- fs：readFile vs createReadStream、pipe 复制文件、背压
- SSE：/events 接口分段推送已跑通
- 环境变量：process.env、.env、node --env-file
- crypto：sha256、randomBytes

### 第 4 周 ✅

- bcrypt 密码哈希（注册/登录已用上）
- JWT 签发/校验 + Authorization: Bearer
- checkAuth 中间件（jwt.verify → req.user）
- pino 结构化日志（auth 路由）

### 第 5 周 ✅（2026-08-28 收尾修复）

- vitest + supertest：18 个测试全绿（tasks 6 / auth 3 / upload 2 / llm service 6 / llm router 1）
- mock 外部请求：vi.stubGlobal（service 层）+ vi.mock（router 层）
- multer 上传：随机文件名、类型/大小限制、/uploads 静态访问
- 安全加固：helmet、CORS 白名单（已修复）、全局限流 + 登录限流
- /healthz + 优雅退出
- 收尾修复：errorHandler 识别 MulterError(413)/zod(400)/CORS(403)、404 兜底、schema completed bug、ESLint+Prettier、JWT_SECRET 轮换并移出 git

> ⚠️ 第 5 周遗留：**Railway 部署上线未确认**（代码已推 GitHub，平台操作待做；线上 JWT_SECRET 要用新密钥）

## 四、第 6 周进行中（本周主题：整合 + 分层重构 + 补漏 + CI）

详细练习见 `学习笔记/阶段一-Node.js后端/第6周学习笔记.md`。核心任务：

1. **四层重构**：`router/` → `routes/`（薄）+ 新建 `controllers/` + 填满 `services/` 空壳（auth/tasks/upload）+ `data/` → `repositories/`
2. **统一错误处理**：AppError + asyncHandler，全项目 0 try/catch，errorHandler 只认 AppError
3. **补漏**：config 模块（zod 校验 env，fail fast）、pino 请求日志（去掉 console.log）
4. **生产验证**：lint / check / test / build / 生产启动全绿
5. **CI**：GitHub Actions 跑 lint + check + test + build，全绿
6. **补第 5 周尾巴**：确认 Railway 上线

遗留小账（可顺手做，见笔记加分练习）：pre-commit 钩子、validate 中间件、service 单测

## 五、学习方法约定（重要）

- 每周：只给**学习笔记 + 练习要求**，不给成品代码；卡住 15 分钟以上再要提示或参考实现
- 参考答案在 `参考答案/week2`，写完后或卡住时对照
- 养成习惯：写完自查（防复制粘贴 bug）、每周收尾闭环、git 提交
- 学习者特点：动手能力强、会串联知识、有安全意识；但偶尔有复制粘贴 bug 和"旧账不清"的习惯，需要提醒闭环（第 4 周 schema bug、第 5 周部署就是教训）

## 六、下次对话的交接语

> 我是正在转岗 AI 应用开发的前端开发者，已经完成第 1-5 周学习（第 5 周部署上线待确认，第 6 周进行中）。请先读 `D:\AI学习\学习进度快照.md`，再读 `D:\AI学习\学习笔记\阶段一-Node.js后端\第6周学习笔记.md`，我们继续学第 6 周：分层重构 + AppError + config + CI。先帮我从练习 1（AppError + asyncHandler）开始验收。
