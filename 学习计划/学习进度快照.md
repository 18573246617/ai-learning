# 学习进度快照（2026-09-08）

> 用途：换新对话时，让新助手快速了解"学到哪了、还剩什么、怎么继续"。新对话里直接说："先读 D:\AI学习\学习进度快照.md，然后继续帮我学第 7 周。"

---

## 一、你是谁、目标是什么

- 在职前端开发者（会 JS/React），目标是转岗 **AI 应用开发 / Agent 开发**
- 全程使用 TypeScript，主学 Express 5；阶段二起：PostgreSQL + Prisma，再往后 LLM 应用 + Agent
- 总计划在 `D:\AI学习\学习计划\AI应用开发转岗学习计划.md`（36 周，5 个阶段）
- 目前处于**阶段二（数据库）第 7 周**：PostgreSQL 上手 + SQL 基础，总进度约 17%（6/36 周完成）

## 二、项目结构（两个学习项目）

阶段一项目：`D:\AI学习\代码练习\阶段一-Node.js后端\`（第 1–6 周，已收官）

```
├── package.json          # pnpm 管理，type: module；dev:week2 / test / lint / check / build 脚本
├── tsconfig.json         # strict + esModuleInterop；typescript 别名（ESLint 用 TS6，别动）
├── env/.env.development  # 环境变量（已移出 git 跟踪）
├── eslint.config.js / .prettierrc.json
├── 参考答案/week2/
├── src/week1~week4       # 练习（已完成）
├── src/week2/            # ★ Express 主应用：四层架构 routes/controllers/services/repositories
└── src/week6/            # 第 6 周重构后的完整结构（config/validate/CI 验证用）
```

阶段二项目（第 7 周新建）：`D:\AI学习\代码练习\阶段二-数据库\`

```
├── docker-compose.yml    # postgres:17 + pgAdmin4 + 数据卷（Docker 装上后可用）
└── week7/sql/            # ★ 本周产出：01 建表 / 02 插入 / 03 更新删除 / 04 查询分页
```

## 三、环境事实（重要）

- Windows，仓库根 `D:\AI学习`，git 工作区干净
- **本机没有 Docker Desktop，也没有原生 PostgreSQL**——第 7 周第 0 步先装环境（选项 A Docker / B 原生 PG17 / C Neon·Supabase 云库，见第 7 周笔记第 2 节）
- 阶段一 CI：`.github/workflows/ci.yml` 已存在（lint + check + test + build）
- Railway 部署是否在线：**以用户自查为准**，阶段一复盘问过但快照未收到最终确认

## 四、已完成的内容（第 1–6 周 ✅）

### 阶段一：Node.js 后端（第 1–6 周，2026-09-07 收官）

- 第 1 周：Node LTS / pnpm / tsx / TS 基础 / 事件循环 / 原生 http
- 第 2 周：Express 5 路由与中间件、手写 myJson、zod、待办 CRUD + auth
- 第 3 周：fs / 流 / pipe / SSE / 环境变量 / crypto
- 第 4 周：bcrypt / JWT / checkAuth / pino
- 第 5 周：vitest + supertest（18 用例全绿）、mock、multer、helmet / CORS / 限流、/healthz、优雅退出
- 第 6 周：四层重构（routes→controllers→services→repositories）、AppError + asyncHandler 全项目 0 try/catch、config fail fast、pino 请求日志、生产构建、GitHub Actions CI
- 阶段一遗留自查：① 总计划文件里第 4–6 周清单还没打勾；② Railway 线上 /healthz 可访问性确认；③ 加分项（pre-commit、service 单测）未做，可后补

## 五、第 7 周进行中（本周主题：PostgreSQL 上手 + SQL 基础）

详细笔记：`学习笔记/阶段二-数据库/第7周学习笔记.md`（含练习 0–5、复盘题、收尾清单）。核心：

1. 环境：Docker 跑 Postgres 17 + pgAdmin（或原生 PG）
2. DDL：CREATE TABLE（主键/IDENTITY/UNIQUE/默认值/外键 CASCADE），命名规范沉淀
3. DML：INSERT/UPDATE/DELETE + RETURNING
4. DQL：SELECT 过滤 / 排序 / count / LIMIT-OFFSET 分页

产出：`week7/sql/` 四个可重跑脚本，git 提交 `feat: 第七周 PostgreSQL 基础（建表 + CRUD + 分页）`。

## 六、学习方法约定（重要）

- 每周：只给**学习笔记 + 练习要求**，不给成品代码；卡住 15 分钟以上再要提示或参考实现
- 阶段一参考答案在 `参考答案/week2`；SQL 练习无标准答案，跑出预期结果并能解释即算过
- 养成习惯：写完自查（防复制粘贴 bug）、每周收尾闭环、git 提交
- 学习者特点：动手能力强、会串联知识、有安全意识；但偶尔有复制粘贴 bug 和"旧账不清"的习惯，需要提醒闭环（第 4 周 schema bug、第 5 周部署就是教训）

## 七、下次对话的交接语

> 我是正在转岗 AI 应用开发的前端开发者，已完成第 1–6 周（阶段一收官：分层重构 + CI 全绿）。现在学阶段二第 7 周：PostgreSQL + SQL 基础，但本机还没装 Docker/PostgreSQL。请先读 `D:\AI学习\学习进度快照.md`，再读 `D:\AI学习\学习笔记\阶段二-数据库\第7周学习笔记.md`，从第 0 步环境准备开始带我做练习。
