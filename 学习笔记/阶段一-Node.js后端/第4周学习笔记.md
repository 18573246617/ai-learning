# 第 4 周学习笔记：认证与用户系统（安全升级版）

> 配套计划：《AI 应用开发转岗学习计划》阶段一（第 4 周）
> 本周目标：把已有的注册/登录升级到安全标准：bcrypt 存密码、JWT 签发/校验/过期、Authorization: Bearer 规范、pino 结构化日志
> 配套项目：`代码练习/阶段一-Node.js后端`（主应用在 `src/week2/`，本周是**升级**现有 auth 模块，不是从零写）
> 练习方式：先自己写，卡住 15 分钟以上再找我要提示或参考实现

---

## 1. 为什么 sha256 存密码不够（升级动机）

你现在的做法：`sha256(密码)` 直接存。

两个致命问题：

1. **相同密码 → 相同哈希**：100 个用户都用 "123456"，库里就有 100 个一模一样的哈希，攻击者一眼就能看出来
2. **sha256 太快 + 无盐**：攻击者可以提前算好常见密码的哈希表（彩虹表），拿到库直接比对，秒破

### bcrypt 怎么解决

- **自动加盐**：每个密码哈希时自动生成随机盐，"123456" 存两次得到两个完全不同的哈希
- **故意慢**：bcrypt 设计上就是慢哈希，攻击者暴力破解的成本指数级上升
- 验证时不需要你管盐：`bcrypt.compare(明文, 哈希)` 内部会自动提取盐重新计算

```ts
import bcrypt from 'bcrypt'

// 注册：哈希后存储（10 是"工作因子"，越大越慢越安全，10 是常用值）
const hash = await bcrypt.hash('123456', 10)

// 登录：明文和库里哈希比对，返回 true/false
const ok = await bcrypt.compare('123456', hash)
```

### 练习 1

- 安装 `bcrypt`，把注册里的 `sha256` 换成 `bcrypt.hash`，登录里的比对换成 `bcrypt.compare`
- 注册两个相同密码的用户，观察库里两个哈希**不一样**（这是 sha256 做不到的）
- 用 `bcrypt.hash('123456', 10)` 感受一下它比 sha256 慢多少（慢是特性，不是 bug）

---

## 2. JWT：签发与校验

### 2.1 为什么换 JWT

你现在的 token（randomBytes）有两个问题：

1. **永不过期**——一旦泄露，攻击者永远能用
2. **要查库**——每个请求都要去数据库里找 token

JWT 是**自包含**的：token 里本身就写着"谁 + 什么时候过期"，服务器校验时**不需要查库**，只验证签名和过期时间。

### 2.2 JWT 长什么样

```
eyJhbGciOiJIUzI1NiJ9.eyJ1c2VybmFtZSI6ImphbWVzIiwiZXhwIjoxNz... .签名
```

三段：`header.payload.signature`

- header：算法信息
- payload：你放的数据（username、exp 过期时间）
- signature：用密钥对前两段签名，防止篡改

### 2.3 签发和校验

```ts
import jwt from 'jsonwebtoken'

// 签发：payload + 密钥 + 过期时间
const token = jwt.sign({ username: 'james' }, SECRET, { expiresIn: '7d' })

// 校验：成功返回 payload，失败（过期/被篡改）抛异常
const payload = jwt.verify(token, SECRET)   // { username: 'james', iat, exp }
```

**关键：SECRET 必须放环境变量**（`process.env.JWT_SECRET`），用 week3 学的 `--env-file` 加载，绝不能写死在代码里。

### 2.4 改造点

- login 成功：不再 `randomBytes`，改成 `jwt.sign` 返回
- checkAuth：不再查库比对 token，改成 `jwt.verify`，成功就把 payload 挂到 `req.user`
- 过期 token 校验失败 → 401（jwt.verify 抛 TokenExpiredError，包 try/catch）

### 练习 2

- 在 `src/week4/` 建一个临时脚本，练习 `jwt.sign` 和 `jwt.verify`：
  - 签一个 3 秒过期的 token，3 秒后 verify 看抛什么错
  - 篡改 token 中间一个字符，verify 看抛什么错
- 再把 login 和 checkAuth 改造成 JWT 版
```ts
import jwt from 'jsonwebtoken'
const JWT_SECRET=process.env.JWT_SECRET
const token =jwt.sign({userName:'james'},JWT_SECRET,{expiresIn:'3s'})
try{
    const payLoad=jwt.verify(token,JWT_SECRET)
    console.log(payLoad, '使用jwt校验token通过');
}catch(error){
   if(error instanceof TokenExpiresError){
      console.log('token已失效');
   }else if(error instanceof JsonWebTokenError){
      console.log('token被篡改或签名不符');
   }else{
      console.log('其他错误');
   }
}


```

---

## 3. Authorization: Bearer 规范

你现在用的是自定义请求头 `x-http-token`。行业标准写法是：

```http
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
```

前端：

```js
headers: { Authorization: `Bearer ${token}` }
```

后端取的时候要去掉前缀：

```tsx
const header = req.headers.authorization          // "Bearer xxxx"
const token = header?.startsWith('Bearer ') ? header.slice(7) : undefined
```

> 注意：`Bearer` 的 B 是大写，`Bearer ` 后面有个空格。

### 练习 3

把 `checkAuth` 改成从 `Authorization: Bearer` 取 token。

---

## 4. localStorage vs cookie（前端存哪）

这是概念题 + 面试题，本周能说清取舍就行，不用写代码。

| | localStorage | cookie（HttpOnly） |
|---|---|---|
| 谁能读到 | 任何 JS 都能读 | JS 读不到，只有浏览器自动带上 |
| 主要风险 | **XSS 攻击**能偷 token | **CSRF 攻击**（cookie 会自动带） |
| 防 XSS | 靠你写代码小心 | 天然免疫 |
| 防 CSRF | 天然免疫（要手动加 header） | 需要 SameSite / CSRF token |
| 适用场景 | 纯前端项目、API 服务 | 传统 Web、同源部署 |

一句话结论：**简单项目用 localStorage + 注意 XSS；要极致安全用 HttpOnly cookie + SameSite。** AI 应用（对接 API 服务）常见做法是 localStorage。

- 注意
- SameSite 是给 cookie 补 CSRF 的：SameSite=Strict 让浏览器跨站请求时不带 cookie，直接断了"图片自动带 cookie"那条路。所以"HttpOnly cookie + SameSite"是双保险。
- 
- 1."localStorage 由 JS 管理、不会自动发送，主要风险是被 XSS 注入的脚本偷走 token，所以免疫 CSRF"
- 2."cookie 由浏览器自动携带，加上 HttpOnly 后 JS 读不到、免疫 XSS，但自动携带的特性引入了 CSRF 风险，需要 SameSite 或 CSRF token 来防"
- 3."简单 API 项目用 localStorage + 防 XSS；要极致安全用 HttpOnly cookie + SameSite"

---

## 5. pino：结构化日志

### 5.1 为什么不用 console.log

`console.log('登录成功', user)` 的输出是一行文本，人看着还行，但**日志系统没法解析**（没法按字段搜索、统计、告警）。

### 5.2 pino 输出 JSON 行

```ts
import pino from 'pino'

const logger = pino()

logger.info({ userId: 1, username: 'james' }, '登录成功')
// 输出：{"level":30,"time":...,"msg":"登录成功","userId":1,"username":"james"}
```

每个字段都能被日志系统索引、过滤、告警。

### 5.3 改造点

- app.ts 里换成 pino 写请求日志（或和现有 logger 并存对比一下）
- auth 路由里加 `logger.info({ username }, '登录成功')`、`logger.warn('登录失败')`
- 错误处理中间件里用 `logger.error(err)` 替代 `console.error`

### 练习 4

- 安装 `pino`，在 app.ts 请求日志和 auth 路由里接入
- 跑一次注册/登录，观察终端输出的 JSON 日志

---

## 6. 本周练习清单（做完打勾）

- [√] bcrypt 注册/登录跑通，两个相同密码的哈希不同
- [√] JWT sign/verify 脚本跑通，过期和篡改的 token 都校验失败
- [√] login 返回 JWT，checkAuth 用 jwt.verify 且支持 Bearer
- [√] 带过期 token 访问受保护接口返回 401
- [√] 能说出 localStorage 和 cookie 的取舍
- [√] pino 输出结构化日志，登录成功/失败都有记录

---

## 7. 常见坑

1. `JWT_SECRET` 写死在代码里 → 必须放 `.env`（用 week3 的 `--env-file`）
2. `jwt.verify` 失败会抛异常（过期、篡改）→ 必须 try/catch，返回 401，不能让它变成 500
3. bcrypt 很慢是特性不是 bug——每个请求都 `bcrypt.hash` 会拖慢服务，注册时才 hash，登录只用 `compare`
4. `Bearer` 的 B 大写、后面有空格，取的时候别写错
5. token 存 localStorage 要意识到 XSS 风险（别用 `innerHTML` 渲染用户输入）

---

## 8. 本周复盘问题（能答上来就过关）

- 为什么 sha256 存密码不够？bcrypt 加了什么？
```ts
 // sha256 没有盐：相同密码 → 相同哈希，攻击者用彩虹表（预计算的哈希表）直接比对就能破解
 // bcrypt 加了盐(salt) + 慢哈希：
 //   1. 每次哈希自动生成随机盐 → 相同密码产生的哈希也不同（防彩虹表）
 //   2. 故意设计得很慢（工作因子 10）→ 暴力破解成本指数级上升
 //   3. 验证时不用管盐：bcrypt.compare 内部自动从哈希里提取盐重新计算
 createHash('sha256').update('密码123').digest('hex')
 const passwordHash = await bcrypt.hash('密码', 10)
 const hash2 = await bcrypt.hash('密码', 10)  // 相同密码 → 哈希不同（sha256 做不到）
 const isMatch = await bcrypt.compare('密码', passwordHash)  // true
```
- JWT 和随机 token 的区别？为什么校验不需要查库？
```ts
// 区别：
// 1. 随机 token 是一段随机字符串，本身不带任何信息，服务器必须查库才能知道对应哪个用户
// 2. JWT 是自包含的：payload 里直接写着"是谁(userName) + 什么时候过期(exp)"
// 校验时只需用密钥验证"签名没被篡改 + 没过期"，信息全在 token 里 → 不需要查库
const token = jwt.sign({ userName: 'james' }, process.env.JWT_SECRET, { expiresIn: '8h' })
try{
  const payload=jwt.verify(token,process.env.JWT_SECRET)
}catch(err){
  if(err instanceof TokenExpiredError){
    return res.status(401).json({
      message:'token已过期'
    })
  } else if (err instanceof jwt.JsonWebTokenError) {
    return res.status(401).json({
      message: 'token被篡改或签名不符'
    })
  }else{
    return res.status(401).json({ message: 'Token 无效' })
  }
}

```
- JWT 过期怎么实现？校验时过期了会发生什么？
```ts
// 实现：jwt.sign 时传 expiresIn，过期时间写进 payload 的 exp 字段（Unix 时间戳）
const token = jwt.sign({ userName: 'james' }, process.env.JWT_SECRET, { expiresIn: '3s' })  // 3 秒后过期
// 校验：过期时 jwt.verify 抛 TokenExpiredError，用 try/catch 捕获后返回 401（不能让它变成 500）

```

- `Authorization: Bearer` 是什么规范？前后端怎么配合？
```ts
  // 1. 前端：请求头设置 headers: { Authorization: `Bearer ${token}` }（B 大写，Bearer 后有空格）
  //2.后端：
 const header = req.headers.authorization  // 注意：headers 是复数，值形如 "Bearer xxx"
 const token = header.startsWith('Bearer ') ? header.slice(7) : undefined  // 去掉 "Bearer " 前缀（7 个字符）
 if (!token) return res.status(401).json({ message: 'Token 不能为空' })
```
- localStorage 和 HttpOnly cookie 各自的优缺点？
```ts
// 1. localStorage：空间大（约 5MB）、JS 可读写
//    → 风险是 XSS：注入的恶意脚本能直接0偷走 token，只能靠写代码小心（别用 innerHTML 渲染用户输入）
//    → 但 localStorage 不会自动随请求发送，天然免疫 CSRF
// 2. cookie（HttpOnly）：JS 读不到（免疫 XSS）、浏览器自动携带
//    → 但"自动携带"引入 CSRF（跨站请求伪造）风险，需要 SameSite=Strict 或 CSRF token 来防
// 注意：HttpOnly 是 cookie 的属性，localStorage 没有 HttpOnly
// 注意：CSRF = 跨站请求伪造（冒充你发请求）；XSS = 跨站脚本攻击（注入脚本偷数据），是两种不同的攻击

```
- pino 和 console.log 的区别？为什么日志要结构化？
// console.log 输出纯文本，日志系统没法按字段搜索、统计、告警
// pino 输出 JSON 行：{"level":30,"time":...,"msg":"登录成功","userId":1}
// 每个字段都能被日志系统索引、过滤、统计、告警，且 pino 性能好（快）
logger.info({ userId: 1, username: 'james' }, '登录成功')

---

> 完成本周内容后，回到总计划把第 4 周的清单打勾，进入第 5 周：测试与文件上传。
