# 复制本文件为 .env.development 并填入真实值（.env* 已加入 .gitignore，不会提交）

# 服务端口
PORT=1300

# JWT 签名密钥（必填）。生成方式：node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=

# CORS 允许的前端来源，多个用英文逗号分隔；留空表示不限制
CORS_ORIGINS=http://localhost:5173
